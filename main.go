package main

/*
 * ARQUIVO: main.go
 * PROPÓSITO: Ponto de entrada (entrypoint) da aplicação CLI.
 * RESPONSABILIDADE:
 * - Inicializar a CLI.
 * - Orquestrar a chamada entre a camada CLI (flags) e a camada Core (lógica).
 * - Gerenciar sinais de sistema (OS signals) para limpeza segura de memória.
 */

func main() {
    // TODO: Implementar inicialização da CLI e chamada do Core
}
